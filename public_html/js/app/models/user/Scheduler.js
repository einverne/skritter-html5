/**
 * @module Skritter
 * @submodule
 * @author Joshua McFarland
 */
define(function() {
    /**
     * @class Scheduler
     */
    var Scheduler = Backbone.Model.extend({
        /**
         * @method initialize
         */
        initialize: function() {
            
        }
    });
    
    return Scheduler;
});