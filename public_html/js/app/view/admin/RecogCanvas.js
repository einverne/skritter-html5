/**
 * @module Skritter
 * @param PromptCanvas
 * @author Joshua McFarland
 */
define([
    'prompt/PromptCanvas'
], function(PromptCanvas) {
    /**
     * RecogCanvas
     */
    var Canvas = PromptCanvas.extend({
        
        
        
    });
    
    
    return Canvas;
});